#include "main.h"

/**
  * mul - Multiplies two integers
  * @i: The first number to multiply
  * @j: The second number to multiply with the first number
  * Return: The result of multiplication between i param and y param
  */
int mul(int i, int j)
{
	return (i * j);
}
